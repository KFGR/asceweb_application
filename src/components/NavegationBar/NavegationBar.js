import React    from "react";
import template from "./NavegationBar.jsx";

class NavegationBar extends React.Component {
  render() {
    return template(this.props);
  }
}

export default NavegationBar;
